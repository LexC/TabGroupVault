# Distribution Builds

Release ZIP files are generated here by `npm run build`, `npm run build:edge`, or `npm run build:chrome`.

Generated archives are ignored by git. Each uploaded store ZIP should contain `manifest.json` at the ZIP root.
